[build]
  command = "npm run build && node netlify-build.js"
  publish = "out"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 